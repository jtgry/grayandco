{
  "name": "hugo",
  "private": true,
  "scripts": {
    "build": "hugo --minify",
    "build:preview": "hugo --buildFuture",
    "start": "hugo server",
    "server": "hugo server",
    "server:with-drafts": "hugo server --buildFuture"
  },
  "dependencies": {
    "autoprefixer": "^9.7.4",
    "netlify-lambda": "^1.6.3",
    "postcss-cli": "^6.1.3"
  }
}
